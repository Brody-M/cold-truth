# Future C8 authorization boundary

## C8o future direct API route

C8o retires `existing_configured_elevenlabs_mcp` for future Cold Truth C8 narration-connectivity tests after two separately authorized, correctly fail-closed attempts produced no usable controlled MP3 path or opaque audio resource. The historical MCP authorization and audit records remain immutable evidence and do not block the new offline contract.

The only future route identifier is `direct_elevenlabs_https_api`. C8o defines a dependency-injected, single-use adapter contract: one future HTTPS POST transport call, one valid bounded MP3-byte response, one in-memory-verified exclusive-create write request for the exact canonical destination, and one non-sensitive audit result. It has no default client, MCP route, SDK, subprocess, environment lookup, configuration lookup, credential discovery, retry, redirect, fallback, polling, cleanup, overwrite, copy, batch, multi-output, or production capability.

The only future credential placeholder names are `COLD_TRUTH_ELEVENLABS_API_KEY` and `COLD_TRUTH_ELEVENLABS_MIA_VOICE_ID`. C8o never reads or checks either name. A future live attempt requires a new separate authorization before Brody may set temporary values locally.

## C8n historical runtime result-shape selection

C8n refined C8m without changing its finalization boundaries. It is now retained as historical MCP-route context only. Its canonical result-shape allowlist and fail-closed classifier document why the completed MCP attempts could not yield a controlled output; they are not the active future route.

For a returned local path, the sole accepted result is one newly produced direct regular MP3 inside the exact disposable C8 output directory. It may be atomically renamed once to the canonical final path. Traversal, absolute/outside paths, nested paths, links, directories, pre-existing files, non-MP3 files, multiple paths, destination existence, and rename failure all block.

For an MCP audio resource, the sole accepted result is one opaque resource passed to a dedicated narrow finalizer. A future separately authorized finalizer may materialize it once at the canonical destination using exclusive create. URLs, downloads, attachments, arbitrary bytes-like values, metadata-only results, non-audio resources, multiple resources, destination existence, and write failure all block. The contract exposes no audio content and provides no network, provider, generic filesystem, or media-processing facility.

Both branches forbid retry, another MCP operation, another finalization action, copy, overwrite, cleanup, deletion, polling, status calls, follow-up actions, temporary media, and audio inspection or processing. C8n is offline-only and creates no new authorization or media.

## C8i staged output finalization

C8i is retained as inactive historical context. Its staging-directory assumption was contradicted by the completed one-time C8 attempt and is not an active future route.

Historically, C8i required one newly created direct regular `.mp3` inside a dedicated staging directory before one atomic rename. That rule is no longer an active success condition; C8m's result-shape branches replace it.

C8i is contract-only. It creates no authorization instance, staging directory, audio, runner, or live filesystem finalizer and invokes no MCP or provider operation.

The sole canonical authorization field for the rename limit is `maximum_local_rename_count`, fixed at `1`. The longer name `maximum_local_finalization_rename_count` is invalid and remains rejected by the closed schema.

## C8h active route

C8h supersedes every C8d–C8g execution/configuration design below. Those sections are retained only as historical context and are not active requirements.

The only active future transport is `execution_transport: existing_configured_elevenlabs_mcp`, limited to exactly one MCP narration operation after a new explicit one-time authorization. Direct HTTP, the local Python runner, opaque configuration slots, Option A environment injection, and Option B private configuration are inactive and forbidden by the active schema. C8h itself invoked no MCP tool and created no authorization instance; the later consumed C8 attempt and safe audit are retained as closed historical artifacts.

Credential, configuration, environment, voice-identifier, endpoint, header, account, and login-state access remain prohibited. Real-case narration remains subject to C4 → C5 → C6.

This directory defines active schema and contract shape plus the closed historical consumed authorization and safe audit. Phase C7 cannot create, validate, consume, or execute a C8 authorization. C8a may validate hypothetical records in memory but cannot create or execute an authorization. No new or unconsumed authorization instance, credential placeholder, endpoint, provider implementation, or live-call command exists here.

The C8b-aligned schema defines C8 as an isolated invented-text provider-connectivity test. It cannot use or bind a C4 approval, C3 Writer/Editor permission, C5 preflight, or C6 authorization because those belong to the C4-gated real-workflow chain. C8 retains only its exact synthetic-text hash, locked Mia-profile hash, C7 request-contract and allowlist hashes, contained output binding, and one-attempt restrictions.

This separation is not a bypass: real-case narration remains permanently subject to C4 and its downstream C5/C6 gates. C8 requires `real_case_content_allowed: false`, `real_script_approval_used: false`, `c4_approval_artifact_allowed: false`, and `c3_fixture_state_must_remain: AWAITING_SCRIPT_APPROVAL`.

C8c adds a distinct non-executable adapter that uses only this isolated schema plus the embedded, independently hashed C7 request-contract object and allowlist. It accepts only the exact test fake, constructs a sanitized request in memory, never mutates or consumes an authorization record, and always returns `execution_authorized: false`.

C8d defines the future live-readiness contract without implementing it. Authorization paths are relative to `automation/`, while the canonical filesystem file is relative to the workspace root under `automation/fixtures/.../disposable_output/`. The future transport and opaque provider-capability source must both be explicitly injected; no default, discovery, live implementation, configuration access, network route, or authorization instance exists in C8d.

C8e clarifies that a future authorization may launch exactly one named top-level local Python runner: `c8_one_time_isolated_provider_runner`. That runner may make at most one in-process HTTP request but cannot spawn any child process, shell, Node, browser, MCP, Codex, or external command. Its opaque configuration request is limited to the abstract `provider_api_credential` and `locked_voice_identifier` slots; no actual variable, path, secret, or capability implementation exists in C8e.

## C8f future configuration plan

C8f remains offline and non-executable. It defines two mutually exclusive mechanisms that a later live C8 authorization may select explicitly:

- Option A, process environment injection: the future one-time runner may read only `COLD_TRUTH_C8_PROVIDER_API_CREDENTIAL` and `COLD_TRUTH_C8_LOCKED_VOICE_IDENTIFIER`. It may not enumerate the environment, try alternate names, or use generic environment access.
- Option B, dedicated ignored local JSON: the human may create `automation/private/c8_one_time_provider_config.json` containing exactly the `provider_api_credential` and `locked_voice_identifier` fields. The future runner may read only that exact path and those exact fields. It may not scan, discover, follow references, try fallback paths, or modify the file.

Neither option is active in C8f. The private file does not exist and is excluded by the workspace `.gitignore`. A later live authorization must choose exactly one option; selecting neither or both fails closed.

Raw values may pass only inside the future one-time runner/transport boundary. They may never be printed, logged, serialized, hashed, returned, reported, included in exceptions or audits, validated, exported, or otherwise exposed. C8f reads no environment variable or configuration file and implements no live client, network transport, provider endpoint, SDK, runner, authorization artifact, or audio operation.
