# C5 fixture output

This directory is reserved for sanitized offline preflight decision records. Requested audio filenames are proposals only. No MP3, WAV, or other media file may be created here during Phase C5.

Phase C6 may add immutable sanitized validation records and explicitly marked `.fake.json` metadata beneath `c6_synthetic_narration_adapter/`. Those records are not audio and must never share an audio/media extension.
